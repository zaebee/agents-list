-- AI Project Manager Database Initialization
-- This script ensures the database and user exist

-- Create database if it doesn't exist (PostgreSQL will handle this)
SELECT 'Database aipm_db ready' as status;